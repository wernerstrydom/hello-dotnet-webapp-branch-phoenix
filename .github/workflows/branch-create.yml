name: Create Environment
on:
  create

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "create"
        run: echo "Creating Environment ${{ github.ref_name }}"